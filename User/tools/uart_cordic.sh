#!/usr/bin/bash



while IFS= read -r line; do
  echo "$line" | awk '{ for(i=NF; i>0; i--) printf("%s", $i); printf("\n") }'
done < ./split_data.txt >> data.txt














































